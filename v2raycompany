<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>V2rayCompany - مدیریت کانفیگ</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #3498db;
            --secondary: #2980b9;
            --success: #27ae60;
            --danger: #e74c3c;
            --dark: #2c3e50;
            --light: #ecf0f1;
        }
        
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: #333;
            line-height: 1.6;
            padding: 20px;
            min-height: 100vh;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }
        
        header {
            background: var(--dark);
            color: white;
            padding: 20px;
            text-align: center;
        }
        
        .logo {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            font-size: 24px;
            margin-bottom: 10px;
        }
        
        .tabs {
            display: flex;
            background: var(--light);
            border-bottom: 2px solid #ddd;
        }
        
        .tab {
            flex: 1;
            padding: 15px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .tab.active {
            background: white;
            border-bottom: 3px solid var(--primary);
            font-weight: bold;
        }
        
        .content {
            padding: 20px;
        }
        
        .tab-content {
            display: none;
        }
        
        .tab-content.active {
            display: block;
            animation: fadeIn 0.5s;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        .connection-status {
            text-align: center;
            padding: 20px;
            margin: 20px 0;
            border-radius: 10px;
            background: #f8f9fa;
        }
        
        .status-connected {
            background: #d4edda;
            color: #155724;
        }
        
        .status-disconnected {
            background: #f8d7da;
            color: #721c24;
        }
        
        .btn {
            display: inline-block;
            background: var(--primary);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 30px;
            cursor: pointer;
            font-size: 16px;
            transition: all 0.3s;
            margin: 5px;
        }
        
        .btn:hover {
            background: var(--secondary);
            transform: translateY(-2px);
        }
        
        .btn-success {
            background: var(--success);
        }
        
        .btn-danger {
            background: var(--danger);
        }
        
        .config-input {
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-family: monospace;
            min-height: 120px;
            resize: vertical;
        }
        
        .stats {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin: 20px 0;
        }
        
        .stat-box {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
            text-align: center;
        }
        
        .stat-value {
            font-size: 24px;
            font-weight: bold;
            color: var(--primary);
        }
        
        .instructions {
            background: #e8f4fc;
            padding: 20px;
            border-radius: 10px;
            margin: 20px 0;
            border-right: 5px solid var(--primary);
        }
        
        .step {
            margin-bottom: 15px;
            display: flex;
            align-items: flex-start;
            gap: 10px;
        }
        
        .step-number {
            background: var(--primary);
            color: white;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }
        
        footer {
            text-align: center;
            padding: 20px;
            color: #777;
            border-top: 1px solid #eee;
        }
        
        @media (max-width: 768px) {
            .tabs {
                flex-direction: column;
            }
            
            .stats {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">
                <i class="fas fa-shield-alt"></i>
                <span>V2rayCompany</span>
            </div>
            <p>مدیریت حرفه‌ای کانفیگ‌های V2Ray</p>
        </header>
        
        <div class="tabs">
            <div class="tab active" data-tab="home">خانه</div>
            <div class="tab" data-tab="config">کانفیگ</div>
            <div class="tab" data-tab="speed">تست سرعت</div>
            <div class="tab" data-tab="help">راهنما</div>
        </div>
        
        <div class="content">
            <!-- صفحه اصلی -->
            <div class="tab-content active" id="home">
                <div class="connection-status status-disconnected" id="connectionStatus">
                    <h3>وضعیت: قطع ارتباط</h3>
                    <p>برای اتصال، روی دکمه زیر کلیک کنید</p>
                    <button class="btn" id="connectBtn">اتصال</button>
                </div>
                
                <div class="stats">
                    <div class="stat-box">
                        <div class="stat-value">0 Mbps</div>
                        <div>سرعت دانلود</div>
                    </div>
                    <div class="stat-box">
                        <div class="stat-value">0 Mbps</div>
                        <div>سرعت آپلود</div>
                    </div>
                    <div class="stat-box">
                        <div class="stat-value">00:00:00</div>
                        <div>زمان اتصال</div>
                    </div>
                    <div class="stat-box">
                        <div class="stat-value">0 MB</div>
                        <div>مصرف داده</div>
                    </div>
                </div>
            </div>
            
            <!-- صفحه کانفیگ -->
            <div class="tab-content" id="config">
                <h2>مدیریت کانفیگ‌ها</h2>
                <p>لینک کانفیگ یا محتوای مستقیم را وارد کنید:</p>
                
                <textarea class="config-input" placeholder="vless://... یا vmess://..."></textarea>
                
                <button class="btn">ذخیره کانفیگ</button>
                <button class="btn btn-success">اتصال</button>
                
                <h3>کانفیگ‌های ذخیره شده</h3>
                <div style="background: #f8f9fa; padding: 15px; border-radius: 10px; margin-top: 15px;">
                    <p>هنوز کانفیگی ذخیره نشده است</p>
                </div>
            </div>
            
            <!-- صفحه تست سرعت -->
            <div class="tab-content" id="speed">
                <h2>تست سرعت اتصال</h2>
                <p>برای اندازه‌گیری سرعت واقعی اتصال خود، دکمه زیر را فشار دهید</p>
                
                <div style="text-align: center; margin: 30px 0;">
                    <div style="font-size: 48px; font-weight: bold; color: var(--primary);">0</div>
                    <div>Mbps</div>
                </div>
                
                <button class="btn" style="display: block; width: 100%;">شروع تست سرعت</button>
                
                <div class="stats" style="margin-top: 30px;">
                    <div class="stat-box">
                        <div class="stat-value">0 ms</div>
                        <div>پینگ</div>
                    </div>
                    <div class="stat-box">
                        <div class="stat-value">0 ms</div>
                        <div>جیتر</div>
                    </div>
                </div>
            </div>
            
            <!-- صفحه راهنما -->
            <div class="tab-content" id="help">
                <h2>راهنمای نصب و استفاده</h2>
                
                <div class="instructions">
                    <h3>نصب روی اندروید</h3>
                    <div class="step">
                        <div class="step-number">1</div>
                        <div>مرورگر Chrome را باز کنید و به آدرس برنامه بروید</div>
                    </div>
                    <div class="step">
                        <div class="step-number">2</div>
                        <div>منوی Chrome (سه نقطه) را باز کنید</div>
                    </div>
                    <div class="step">
                        <div class="step-number">3</div>
                        <div>گزینه "Add to Home screen" را انتخاب کنید</div>
                    </div>
                </div>
                
                <div class="instructions">
                    <h3>نصب روی آیفون</h3>
                    <div class="step">
                        <div class="step-number">1</div>
                        <div>مرورگر Safari را باز کنید و به آدرس برنامه بروید</div>
                    </div>
                    <div class="step">
                        <div class="step-number">2</div>
                        <div>دکمه Share (مربع با فلش) را بزنید</div>
                    </div>
                    <div class="step">
                        <div class="step-number">3</div>
                        <div>گزینه "Add to Home Screen" را انتخاب کنید</div>
                    </div>
                </div>
                
                <div class="instructions">
                    <h3>پشتیبانی از پروتکل‌ها</h3>
                    <ul style="padding-right: 20px;">
                        <li>VLESS</li>
                        <li>VMess</li>
                        <li>Shadowsocks</li>
                        <li>WebSocket</li>
                    </ul>
                </div>
            </div>
        </div>
        
        <footer>
            <p>V2rayCompany - نسخه ۱.۰.۰</p>
        </footer>
    </div>

    <script>
        // مدیریت تب‌ها
        document.querySelectorAll('.tab').forEach(tab => {
            tab.addEventListener('click', () => {
                // حذف کلاس active از همه تب‌ها
                document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
                document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
                
                // اضافه کردن کلاس active به تب انتخاب شده
                tab.classList.add('active');
                document.getElementById(tab.dataset.tab).classList.add('active');
            });
        });
        
        // مدیریت اتصال
        const connectBtn = document.getElementById('connectBtn');
        const connectionStatus = document.getElementById('connectionStatus');
        
        let isConnected = false;
        
        connectBtn.addEventListener('click', () => {
            isConnected = !isConnected;
            
            if (isConnected) {
                connectionStatus.className = 'connection-status status-connected';
                connectionStatus.innerHTML = `
                    <h3>وضعیت: متصل</h3>
                    <p>اتصال با موفقیت برقرار شد</p>
                    <button class="btn btn-danger" id="connectBtn">قطع اتصال</button>
                `;
                // اضافه کردن مجدد ایونت لیستنر به دکمه جدید
                document.getElementById('connectBtn').addEventListener('click', connectBtn.click);
            } else {
                connectionStatus.className = 'connection-status status-disconnected';
                connectionStatus.innerHTML = `
                    <h3>وضعیت: قطع ارتباط</h3>
                    <p>برای اتصال، روی دکمه زیر کلیک کنید</p>
                    <button class="btn" id="connectBtn">اتصال</button>
                `;
                // اضافه کردن مجدد ایونت لیستنر به دکمه جدید
                document.getElementById('connectBtn').addEventListener('click', connectBtn.click);
            }
        });
        
        // شبیه‌سازی تست سرعت
        const speedTestBtn = document.querySelector('#speed .btn');
        const speedValue = document.querySelector('#speed div div');
        
        speedTestBtn.addEventListener('click', () => {
            speedTestBtn.textContent = 'در حال تست...';
            speedTestBtn.disabled = true;
            
            // شبیه‌سازی تست سرعت
            let speed = 0;
            const interval = setInterval(() => {
                speed += Math.random() * 10;
                speedValue.textContent = speed.toFixed(1);
                
                if (speed > 75) {
                    clearInterval(interval);
                    speedTestBtn.textContent = 'شروع تست سرعت';
                    speedTestBtn.disabled = false;
                }
            }, 100);
        });
    </script>
</body>
</html>